const { app } = require("../server");

describe("API", () => {
    describe("/docs", () => {
        it("Returns a HTTP 200 response", () => {
            expect(true).toBe(false);
        });
    });
});
